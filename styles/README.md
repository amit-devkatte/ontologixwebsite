# Styles

Global Tailwind v4 tokens live in `app/globals.css` so Next.js can load them once from the root layout.

The design system is also mirrored as TypeScript data in `config/design-system.ts` for documentation, audits, and future design-system pages.
